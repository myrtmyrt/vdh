---
export interface Props {
    color: "primary" | "secondary"
}
const {color}=Astro.props;

---

<button class={(color == "primary" ? "bg-primary" : "bg-myGreen")+" rounded-full text-white px-4 py-1 w-min hover:-rotate-1 hover:scale-105 transition-all"} >
    <slot/>
</button>